export const closeAlerts = () => {
   

    //cy.get('a[title="Close Alert error alert"]').click().should('not.be.visible')
    //cy.get('.global-alert-close-container > a').click({ multiple: true }).should('not.be.visible')
    //cy.get('a[title="Close Dismissible alert alert"]').click().should('not.be.visible')
    //cy.get('a[title="Close Global warning alert alert"]').click().should('not.be.visible')
   // cy.get('a[title="Close Global information alert alert"]').click().should('not.be.visible')
}