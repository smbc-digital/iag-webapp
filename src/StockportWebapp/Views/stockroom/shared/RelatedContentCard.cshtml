@model SubItem;
@{
    string imageSrc = !string.IsNullOrEmpty(Model.TeaserImage)
        ? Model.TeaserImage
        : Model.Image;
}

<li class="card-item">
    <a class="card-item__link" href="@Model.NavigationLink" aria-label="Read more about @Model.Title">
        <div class="card-item__content-wrapper">
            <partial name="CardImage" model="imageSrc" />
            
            <div class="card-item__container">
                <h3 class="h-l">@Model.Title</h3>

                <div>                
                    <p class="card-item__content-teaser">@Model.Teaser</p>
                    <span class="btn card-item__container-btn">Read more</span>
                </div>
            </div>
        </div>
    </a>
</li>