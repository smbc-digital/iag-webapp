@model InlineQuote

<div class="profile-quote">
    <h3>@Model.Author</h3>
    <div class="profile-quote--border">
        <div class="profile-quote__text">
                <p><span class="open h-s fa fa-quote-left" aria-hidden="true"></span> @Model.Quote <span class="h-s fa fa-quote-right" aria-hidden="true"></span></p>
        </div>
    </div>
</div>