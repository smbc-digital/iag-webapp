@model NavCardList

<ul class="navcard-wrapper navcard-wrapper--no-margin">
    @foreach (var navCard in Model.Items)
    {
        <li class="navcard navcard--width-25 navcard--grey navcard--border">
            <partial name="TopicNavCard" model="navCard" />
        </li>
    }
</ul>
