@model List<Trivia>

@if (Model is not null)
{
    <ul class="trivia__list trivia__list--profile">
        @foreach(Trivia trivia in Model)
        {
            <li class="trivia__container trivia__container--profile">
                @if(!string.IsNullOrEmpty(trivia.Link))
                {
                    <a href="@trivia.Link" class="trivia__link" data-cy="trivia-list-link">
                        <div>
                            <span class="@trivia.Icon trivia__icon"></span>
                        </div>
                        
                        <div class="trivia__body">
                            @Html.Raw(trivia.Body)
                        </div>
                    </a>
                }
                else {
                    <div class="trivia__text" data-cy="trivia-list-text">
                        <div>
                            <span class="@trivia.Icon trivia__icon"></span>
                        </div>
                        
                        <div class="trivia__body">
                            @Html.Raw(trivia.Body)
                        </div>
                    </div>
                }
            </li>
        }
    </ul>
}