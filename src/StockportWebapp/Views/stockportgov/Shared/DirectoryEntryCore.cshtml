@model DirectoryEntryViewModel
@if (!string.IsNullOrEmpty(Model.DirectoryEntry.Image))
{
    <img src="@Model.DirectoryEntry.Image?w=350&q=89&fm=webp"
         class="directory__logo"
         alt=""
         width="350"
         height="150">
}

<h2 class='@(!string.IsNullOrEmpty(Model.DirectoryEntry.Image) ? "directory__profile-image" : "directory__profile")'>Our details</h2>
<div class="directory__organisation">
    <h3 class="h-m">Organisation</h3>
    @if (string.IsNullOrEmpty(Model.DirectoryEntry.Provider))
    {
        <p>@Model.DirectoryEntry.Name</p>
    }
    else
    {
        <p>@Model.DirectoryEntry.Provider</p>
    }
</div>

<div class="directory__address">
    @if (!string.IsNullOrEmpty(Model.DirectoryEntry.Address))
    {
        <h3 class="h-m">Address</h3>
        <div>@Html.Raw(Model.DirectoryEntry.Address)</div>
    }
</div>

<div class="directory__contact">
    <partial name="DirectoryEntryContacts" />
    <partial name="DirectoryEntrySocials" />
</div>