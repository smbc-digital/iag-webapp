@model ContentBlock
@if (Model is not null && Model.SubItems?.Any() == true)
{
    <section class="section section--white">
        <div class="grid-container">
            <div class="grid-100">
                <div class="placeholder placeholder-bg-grey placeholder-small">
                    @foreach(ContentBlock contentBlock in Model.SubItems)
                    {
                        <div class="placeholder placeholder-bg-green placeholder-small placeholder-inline-margin">
                            <h2>PROFILE CARDS</h2>
                            <p>@contentBlock.Title</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>
}