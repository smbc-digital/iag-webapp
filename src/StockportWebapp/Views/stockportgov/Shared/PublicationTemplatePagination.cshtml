@model PublicationTemplateViewModel

<nav class="article-pagination article-pagination--margin-top" aria-label="Pagination">
    <div class="article-pagination__previous article-pagination__previous--publication">
        @if (Model.HasPrevious())
        {
            PaginationTarget prev = Model.GetPrevious();
            <a href="@prev.Url">
                <div class="article-pagination__chevron">
                    <span class="fa fa-angle-left article-pagination__icon article-pagination__icon--ltr" aria-hidden="true"></span>
                    <span class="fa fa-angle-right article-pagination__icon article-pagination__icon--rtl" aria-hidden="true"></span>
                </div>

                <h3>Previous</h3>
                
                @if (prev.IsSection)
                {
                    if (!prev.SamePageAsCurrent)
                    {
                        <p class="publication-template__pagination--no-margin"><strong>@prev.PageTitle</strong></p>
                    }

                    <p>@prev.SectionTitle</p>
                }
                else
                {
                    <p>@prev.PageTitle</p>
                }
            </a>
        }
    </div>

    <div class="article-pagination__next article-pagination__next--publication">
        @if (Model.HasNext())
        {
            PaginationTarget next = Model.GetNext();
            <a href="@next.Url">
                <h3>Next</h3>
                
                @if (next.IsSection)
                {
                    if (!next.SamePageAsCurrent)
                    {
                        <p class="publication-template__pagination--no-margin"><strong>@next.PageTitle</strong></p>
                    }

                    <p>@next.SectionTitle</p>
                }
                else
                {
                    <p>@next.PageTitle</p>
                }

                <div class="article-pagination__chevron">
                    <span class="fa fa-angle-right article-pagination__icon article-pagination__icon--ltr" aria-hidden="true"></span>
                    <span class="fa fa-angle-left article-pagination__icon article-pagination__icon--rtl" aria-hidden="true"></span>
                </div>
            </a>
        }
    </div>
</nav>

<hr class="article-hr" />