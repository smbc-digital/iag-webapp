@{
    var host = Context.Request.Host.Host;
    var cspBuilder = new ContentSecurityPolicyBuilder();
    var allowedContent = cspBuilder.BuildPolicy();
}

<meta http-equiv="Content-Security-Policy" content="@Html.Raw(allowedContent)" />
