@using System.Diagnostics
@model StockportWebapp.QuestionBuilder.Entities.Question

<style>
    .input-error-content {
        padding: 5px 0 5px 10px;
    }

        .input-error-content:before {
            top: -45px;
        }
</style>


<span data-questionid="@Model.QuestionId" aria-atomic="true" role="alert" style="display: none;" class="input-error-content field-validation-error"><span>@(Model.ValidationResult?.Message ?? "")</span></span>


@*@if (Model.ValidationResult?.Message != null || Model.ValidationResult?.Message != string.Empty)
{
    @if (string.IsNullOrEmpty(Model.ValidationResult?.Message) && Model.ValidationResult?.Message != "No message")
     {
         <span data-questionid="@Model.QuestionId" aria-atomic="true" role="alert" style="display: none;" class="input-error-content field-validation-error"><span>@(Model.ValidationResult?.Message ?? "")</span></span>
     }
    
    @if (Model.ValidationResult?.Message != string.Empty)
    {
        <span data-questionid="@Model.QuestionId" aria-atomic="true" role="alert" style="display: none;" class="input-error-content field-validation-error"><span>@(Model.ValidationResult?.Message ?? "")</span></span>
    }
}*@

