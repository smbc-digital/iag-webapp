@model SubItem
@{
 
}
@if (Model is not null)
{
    <section class="section section--white">
        <div class="grid-container">
            <div class="grid-100">
                <div class="placeholder placeholder-bg-pink placeholder-large">
                    <div>
                        <h2>BODY</h2>
                        @Html.Raw(Model.Body)
                    </div>
                </div>
            </div>
        </div>
    </section>
}